# Core runtime
smbus2>=0.4

# USB-I2C backends
pyftdi>=0.54            # direct FT232H backend
adafruit-blinka>=8.0    # Blinka FT232H backend (set BLINKA_FT232H=1)

# Raspberry Pi GPIO (only on Linux)
RPi.GPIO; platform_system == "Linux"

# Optional web UI/API
flask>=3.0
